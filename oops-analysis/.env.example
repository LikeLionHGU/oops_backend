OPENAI_API_KEY=sk-여기에-키
# 지원 크레딧이 특정 조직에 지급된 경우에만 채우세요 (org-...)
OPENAI_ORG_ID=
OPENAI_PROJECT_ID=
WHISPER_MODEL=whisper-1
WORK_DIR=./workdir

# 영상 길이 상한(초). 기본 5400 = 90분.
# 길수록 비용과 시간이 크게 늘어난다.
MAX_DURATION_SEC=5400

# OCR 로 인식할 최대 프레임 수. 영상이 길면 간격이 자동으로 늘어난다.
# 올리면 더 촘촘히 보지만 그만큼 느려진다.
MAX_OCR_FRAMES=300
OCR_LANG=korean
